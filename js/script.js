document.addEventListener("DOMContentLoaded", () => {
	const test = document.querySelectorAll("input");
	
		console.log(test);
		test.forEach(function () {
			const numA1 = parseInt(
				form.querySelector('input[name="a1"]').value
			);
			const numA2 = parseInt(
				form.querySelector('input[name="a2"]').value
			);
			const numA3 = parseInt(
				form.querySelector('input[name="a3"]').value
			);
			console.log(mass);
			
	});
});
function mac() {
	alert(`${numA1} ${numA2} ${numA3}`);
}
